//Odd
#include <stdio.h>

int main() {
    int count = 1;

    while(count <= 49) {
        printf("%d ", count);
        count = count + 2;
    }
    return 0;
}

//Even
#include <stdio.h>

int main() {
    int count = 2;

    while(count <= 49) {
        printf("%d ", count);
        count = count + 2;
    }
    return 0;
}
